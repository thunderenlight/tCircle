<h1>Your Boards</h1>

<%= link_to("New Board", new_board_path) %>

<% @boards.each do |board| %>
	<div class="center">
		<h2><%= board.title %></h2>
		<p><%= board.description %></p>
		<p>Includes <%= board.interests.count %> interests</p>
	</div>
<% end %>